specVersion: "v1beta"
serviceId: "server-identity"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "server-identity-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
